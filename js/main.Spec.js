describe('Calculator Test Suite', () => {

  beforeEach(() => { })

  it("add funtion is defined", () => {
    expect(calculator.add).toBeDefined()
  })

  it("2 plus 2 equals 4", () => {
    expect(calculator.add(2, 2)).toBe(4)
  })

  it("2 plus 2 not equals 5", () => {
    expect(calculator.add(2, 2)).not.toBe(5)
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(7)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(3)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

  it("adding with delay", (done) => {
    calculator.asyncAdd(2, 2, (result) => {
      expect(result).not.toBe(5)
      done()
    })
  })

})